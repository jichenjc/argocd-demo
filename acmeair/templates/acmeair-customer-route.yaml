apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "acmeair.fullname" . }}-customer-route
spec:
  #host: {{ include "acmeair.fullname" . }}.apps.ocp4.demointel.mpl
  host: {{ .Values.host }}
  path: /customer
  to:
    kind: Service
    name: {{ include "acmeair.fullname" . }}-customer-service
    weight: 100
  wildcardPolicy: None
