apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: flight-db
spec:
  gateways:
  - acmeair-gateway
  hosts:
  - 'acmeair-flight-db'
  tcp:
  - match:
    - port: 27017
    route:
    - destination:
        host: acmeair-flight-db
        port:
          number: 27017
