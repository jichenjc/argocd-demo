apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: booking-db
spec:
  gateways:
  - acmeair-gateway
  hosts:
  - 'acmeair-booking-db'
  tcp:
  - match:
    - port: 27017
    route:
    - destination:
        host: acmeair-booking-db
        port:
          number: 27017
